services:
  - type: web
    name: hermes-telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_owl_tools.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
